import numpy as np

def clamp(n, minn, maxn):
    return max(min(maxn, n), minn)

CAPTURE_WIDTH = 640
CAPTURE_HEIGHT = 360

RESIZED_WIDTH = 160
RESIZED_HEIGHT = 90

PLAYER_COUNT = 6
PLAYERS_IN_ROW = int(PLAYER_COUNT / 2)

MIN_TRANSLATE_FREQUENCY = 250
MAX_TRANSLATE_FREQUENCY = 1600

ROTATE_FREQUENCY = 1600

BALL_LOWER_BOUND = np.array([80, 0, 180])
BALL_UPPER_BOUND = np.array([170, 90, 255])

PLAYER_LOWER_BOUND = np.array([90, 0, 90])
PLAYER_UPPER_BOUND = np.array([180, 70, 180])

TRANSLATION_STOP_PERCENTAGE = 0.035

TRANSLATION_PID_KP = 200
TRANSLATION_PID_KI = 0.1
TRANSLATION_PID_KD = 10